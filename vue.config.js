module.exports = {
    // Source map in production has been (temporarily?) removed due to Webpack's
    // `SourceMapGenerator` throwing an error when running `npm run build`.
    // Error is of the same nature as webpack-contrib/babel-minify-webpack-plugin#68
    productionSourceMap: false,
};